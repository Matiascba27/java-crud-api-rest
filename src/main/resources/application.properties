spring.jpa.hibernate.ddl-auto=update
spring.application.name=apirest
server.port=8080
spring.config.import=optional:file:.env[.properties]
spring.datasource.url=${SPING_DATASOURCE_URL}
spring.datasource.username=${SPING_DATASOURCE_USERNAME}
spring.datasource.password=${SPING_DATASOURCE_PASSWORD}